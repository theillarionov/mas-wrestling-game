{
	"name": "backend",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "nodemon --inspect ./src/signalling-server.ts",
		"build": "npx tsc",
		"serve": "node ./dist/signalling-server.js",
		"production": "npm run build && npm run serve"
	},
	"dependencies": {
		"ws": "^8.8.0"
	},
	"devDependencies": {
		"@types/webrtc": "^0.0.32",
		"@types/ws": "^8.5.3",
		"nodemon": "^2.0.18",
		"ts-node": "^10.8.1",
		"typescript": "^4.7.4"
	},
	"nodemonConfig": {
		"execMap": {
			"ts": "node --require ts-node/register/transpile-only"
		}
	}
}
